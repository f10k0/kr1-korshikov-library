<Window x:Class="LibraryManagement.Views.BookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование книги" Height="400" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Название:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Book.Title}" 
                 Margin="5" Padding="3" Height="30"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Автор:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="1" Grid.Column="1" 
                  ItemsSource="{Binding Authors}" 
                  SelectedItem="{Binding SelectedAuthor}"
                  DisplayMemberPath="LastName"
                  Margin="5" Padding="3" Height="30"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Жанр:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="2" Grid.Column="1" 
                  ItemsSource="{Binding Genres}" 
                  SelectedItem="{Binding SelectedGenre}"
                  DisplayMemberPath="Name"
                  Margin="5" Padding="3" Height="30"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Год издания:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Book.PublishYear}" 
                 Margin="5" Padding="3" Height="30"/>

        <Label Grid.Row="4" Grid.Column="0" Content="ISBN:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Book.ISBN}" 
                 Margin="5" Padding="3" Height="30"/>

        <Label Grid.Row="5" Grid.Column="0" Content="Количество:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Book.QuantityInStock}" 
                 Margin="5" Padding="3" Height="30"/>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom"
                    Margin="0,20,0,0">

            <Button Content="OK" IsDefault="True" Click="OkButton_Click" 
                    Width="80" Height="30" Margin="5"/>
            <Button Content="Отмена" IsCancel="True" 
                    Width="80" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>